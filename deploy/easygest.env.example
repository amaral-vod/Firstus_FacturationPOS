# Copier en deploy/easygest.env (ne pas committer — dans .gitignore)
# hPanel → Avancé → Accès SSH → hostname + port (souvent 65002)

SSH_HOST=srvXXXX.hstgr.cloud
SSH_USER=u648457710
SSH_PORT=65002
# SSH_KEY=/home/frioldfr/.ssh/id_ed25519

REMOTE_PATH=/home/u648457710/domains/easygest.org/public_html/facturation

# MySQL Hostinger (hPanel → Bases de données)
DB_DATABASE=u648457710_firstus
DB_USERNAME=u648457710_firstus
DB_PASSWORD=

# Mettre à 1 pour importer deploy/firstus_pos.sql.gz après déploiement
IMPORT_DB=1
